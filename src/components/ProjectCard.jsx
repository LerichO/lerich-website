import { useState } from "react"

function ProjectCard({title, description, image, tech, github, deployment}) {
    return (
        <div className="w-64 h-40 bg-[#3F3F3F]">
            {title}
            🛠️ WIP 🛠️        
        </div>
    )
}

export default ProjectCard